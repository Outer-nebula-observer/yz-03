# 契约自检清单（long_short_term_memory · 已逐项核对）

> 对照《接入自检清单.md》逐项勾选；证据来自 `自检结果.txt`（2026-09-18 离线自检，全部通过）。

## 契约合规

- [x] `engine_plugin` 是 `MemoryEnginePlugin` 实例 —— `server/engines/long_short_term_memory/__init__.py` 直接导出模块级 `engine_plugin`，不再 try/except 吞成 None（返工清单 B2）
- [x] `name` 唯一，小写下划线 —— `long_short_term_memory`
- [x] `engine_label` 非空 —— `长短期记忆`
- [x] `engine_color` 是 `#rrggbb` —— `#2E86AB`
- [x] `contract_version == "1.0.0"`
- [x] `capabilities` 与已实现方法一致 —— 只声明检索能力：`supports_ingest=False`、`supports_delete=False`、`supports_generate=False`、`supports_stream=False`、`supports_browse=False`；未实现的 ingest/generate 方法不声明

## 检索结果（search）

- [x] 每条返回 `content` / `score` / `source_file` / `chunk_id` / `engine` 五字段（另附 `metadata`）
- [x] `score` 归一化 0~1 —— 实测抽查无越界
- [x] `source_file` 非系统绝对路径 —— 取值如 `sdk_check`、`[memsys:fact]`，无 `D:\`、`/home/` 等

## 运行约束

- [x] 无同步 IO / 重计算阻塞事件循环 —— `search()` 使用 `asyncio.to_thread(self._search_sync, ...)`
- [x] 不吞异常返回空列表 —— 检索异常向上抛，交路由器熔断器；本地已用注入异常验证
- [x] `check_availability` 带 TTL —— `AVAILABILITY_TTL = 60.0` 秒，已本地验证缓存与过期重探

## 生成（本引擎不支持，故不适用）

- [x] `supports_generate=False` / `supports_stream=False` —— 不实现 `generate` / `generate_stream`；
      服务端 SSE 层按契约自动补 `engine_done(status="unsupported")`

## 验证

- [x] `python scripts/validate_engine.py long_short_term_memory` 通过（退出码 0）—— 见 `自检结果.txt` 第 1 节
- [ ] `python scripts/smoke_test_engine.py long_short_term_memory` —— **待集成方在服务端启动后执行**（HTTP 冒烟需服务在跑）
- [ ] `GET /api/v2/memory-engine/engines` 能看到该引擎 —— **同上，待集成方验证**

## 交付物约定

- [x] 目录名 = 引擎 `name` = `long_short_term_memory`（小写下划线、不以下划线开头、不命中保留名黑名单）
- [x] 目录内含 `__init__.py`（直接导出 `engine_plugin`）与 `engine.py`，另附 `README.md`
- [x] 依赖清单已附：`依赖清单.txt`（零第三方依赖，仅标准库）
- [x] 核心包 `memsys/` 随引擎目录交付，且引擎内部使用相对导入 `.memsys`（返工清单 B1）

## 数据落位（本课题特有）

- [x] 不依赖静态检索数据文件 —— 记忆由运行期记忆进化写入 SQLite/向量索引
- [x] 如需真模型，通过 `.env` / 环境变量注入 `LSTM_LLM`、`LSTM_EMBEDDING`（返工清单 B4）
